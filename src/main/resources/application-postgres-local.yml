spring:
  application:
    name: defaults
  mail:
    use-notification-service: false
  datasource:
    monitoring-query: true
    driver-class-name: org.postgresql.Driver
    url: jdbc:postgresql://localhost:5432/postgres?currentSchema=defaults
    username: postgres
    password: Mery1029384756$
  liquibase:
    change-log: classpath:/db/postgres/changelog.xml
    url: jdbc:postgresql://localhost:5432/postgres?currentSchema=defaults
    user: postgres
    password: Mery1029384756$
    contexts: prod,dev
    database-change-log-table: DATABASECHANGELOG
    database-change-log-lock-table: DATABASECHANGELOGLOCK
  sql:
    init:
      mode: always
      platform: postgres

integration:
  kafka:
    crs:
      enabled: false
    drp:
      enabled: false

audit:
  enabled: false
  events:
    prefix: DFBS
  kafka:
    bootstrap.server: localhost:9092
    topic: audit
    ssl:
      enabled: false
      truststore-type: 'PKCS12'
      keystore-type: 'PKCS12'

# Настройки трассировки
ru:
  vtb:
    tstr:
      client-starter:
        enabled: false
opentracing:
  jaeger:
    service-name: CURS.${spring.application.name}.ds1-genr01-curs-dev1
    enable-b3-propagation: false
    enabled: false

# Сервис "CURS. Контроль данных"
data-control:
  enabled: false
  rabbitmq:
    host: dfcurs-rbc001lk.corp.dev.vtb
    port: 5671

server:
  port: 8080
